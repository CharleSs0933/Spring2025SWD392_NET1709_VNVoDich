import { ChildrenDropTable } from "@/types";
import React from "react";

const ChildrenTable = ({ child, onEdit, onDelete }: ChildrenDropTable) => {
  return <div>
    
  </div>;
};

export default ChildrenTable;
